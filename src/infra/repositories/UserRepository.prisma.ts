// import { UserNotFound } from "../../application/interfaces";
// import { IUserProps } from "../../domain/entities/User";
// import { User } from "@prisma/client";
// import { Request } from "express";

// export interface UserRepository {
//     getAll(req: Request): Promise<User[]>;
//     createUser(user: IUserProps): Promise<User>;
//     findById(id: string): Promise<User | UserNotFound>;
//     updateUser(id: string, user: IUserProps): Promise<User | UserNotFound>;
//     deleteUser(id: string): Promise<User | UserNotFound>;
// }
